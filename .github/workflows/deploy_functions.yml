name: Deploy Firebase Functions

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Code
        uses: ./.github/actions/setup_code

      - name: Deploy Firebase Functions
        uses: ./.github/actions/firebase_function
        with:
          firebase_token: ${{ secrets.FIREBASE_TOKEN }}
          working-directory: ./functions
